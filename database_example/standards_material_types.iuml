!define STANDARDS_MATERIAL_TYPES

!ifndef TABLES
!include ../Templates/Tables.cuml
!endif

enumeration(material_types) {
    {field} primary(id)
    {field} name: varchar(20) UNIQUE NOT NULL
    !ifndef HIDE
        {method} material types, e.g. flourophore, etc.
    !endif
}